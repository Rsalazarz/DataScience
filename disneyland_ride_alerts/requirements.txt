# Dependencies for the Google Cloud Function (main.py).
# The CLI (ride_alerts.py) itself needs NONE of these — it is stdlib-only.
functions-framework==3.*
google-cloud-storage==2.*
# Timezone data so the digest can show Pacific time via zoneinfo.
tzdata
